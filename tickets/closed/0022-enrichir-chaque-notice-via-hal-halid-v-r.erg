%erg 0.1
Title: Enrichir chaque notice via HAL (halId + vérif métadonnées)
Created: 2026-06-23
Author: Minh Ha-Duong
Closed: autoclosed — PR #50

--- log ---
2026-06-23T07:43Z Minh Ha-Duong created
2026-06-23T07:43Z claude note Prototype HAL validé en revue 0019 (hal-00624329 a confirmé la notice Godard/Futuribles). À industrialiser en swarm. Famille avec [[0023]] [[0024]] ; bénéficie de [[0021]].
2026-06-24T12:00Z claude note Harnais src/enrich_hal.py (recherche HAL → appariement corroboré Jaccard+auteur+année → diff, lecture seule, rapport ; PR #44). Passage complet sur les 579 notices éligibles : 89 appariées (~15 %), couverture concentrée sur les articles CIRED récents + le fonds LEESU (Thévenot électrochimie/biocapteurs, Azimi/Rocher chimie de l'eau). Corroboration auteur+année : 0 faux positif, même les 4 sim<0,8 sont corrects (titres tronqués, pas mauvais doc). Un swarm parallèle s'est avéré inutile — une passe séquentielle suffit sans marteler l'API.
2026-06-24T12:10Z Minh Ha-Duong note Convention validée : halId en ligne « HAL: <id> » ajoutée à l'Extra (jamais d'écrasement). src/gen_hal_corrections.py génère les corrections depuis le rapport ; écrites via apply_corrections.py (ledger outputs/hal_corrections_proposals.json, backup outputs/enrich_hal_backup.json). 88 notices enrichies : halId + champs vides ajoutés (DOI ×30, volume ×12, revue ×6, numéro ×5). Pages EXCLUES de l'auto-ajout (HAL parfois tronqué, ex. « 51-99 » au lieu de « 5199-5207 ») + divergences laissées en revue. Vérifié live : 88 lignes HAL, 0 doublon. Les ~490 non-appariées ne sont pas dans HAL (vieux docs/revues étrangères/grise).

2026-06-24T09:26Z Minh Ha-Duong closed — autoclosed — PR #50
--- body ---
## Context

Enrichir systématiquement chaque notice du catalogue via **HAL** (archives
ouvertes). Pour chaque notice : rechercher dans HAL par titre + auteur (+ année),
apparier le bon résultat, ajouter le **halId** et **vérifier/corriger** les
métadonnées (revue, volume, numéro, pages, date, DOI) d'après HAL.

Prototype déjà validé (revue 0019) : l'API `api.archives-ouvertes.fr/search`
renvoie titre, auteurs, revue, numéro, pages, date, docType, doiId, halId — ex.
`hal-00624329` a confirmé exactement la notice Godard/Futuribles 209. Reste à
industrialiser en **swarm** (fan-out d'agents sur des lots de notices).

Famille d'enrichissement avec [[0023]] (OpenAlex) et [[0024]] (Google) ;
bénéficie de [[0021]] (noms normalisés). Les trois partagent un harnais commun :
rechercher → apparier (réutiliser `match_untyped`/`diff_recueil`) → diff →
**revue humaine** → écrire via `src/apply_corrections.py`.

## Relevant files

- `src/apply_corrections.py` — écriture Zotero (backup, version)
- `src/match_untyped.py`, `src/diff_recueil.py` — appariement + diff de champs
- `src/reconcile_zotero.py` — collecte des notices

## Actions

1. Harnais de recherche HAL (API, lib standard) → candidats structurés.
2. Appariement notice ↔ résultat HAL (titre+auteur+année), avec seuil et
   corroboration (cf. leçon 0019 : Jaccard + auteur/année, pas le containment).
3. Diff métadonnées (HAL → notice) + halId ; rapport pour revue.
4. Swarm : fan-out d'agents sur lots ; agréger les diffs.
5. Après revue : écrire halId + corrections (dry-run, backup).

## Test

- Parsing d'un résultat HAL → champs ; appariement sur fixtures (vrai/faux).

## Verification

- [x] halId ajouté à chaque notice trouvée dans HAL (88 ; 1 déjà à jour)
- [x] Diffs de métadonnées revus avant écriture (sim audité, pages exclues, divergences en revue)
- [x] Corrections appliquées (backup enrich_hal_backup.json), rapport des notices sans correspondance (enrich_hal_report.md)

## Invariants

- Appariement corroboré (jamais titre générique seul) ; revue avant écriture.
- Dry-run par défaut ; backup ; sens unique source → notice.

## Exit criteria

- [x] Catalogue enrichi via HAL (88 halId + champs vérifiés, rapport ; ~490 non couvertes car absentes de HAL)
- [x] `make check` passe
